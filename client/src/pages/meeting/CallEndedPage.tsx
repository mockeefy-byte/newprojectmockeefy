import { CheckCircle, Home, RotateCcw } from 'lucide-react';

export function CallEndedPage() {
  return (
    <div className="relative w-full h-screen bg-[#121212] overflow-hidden">
      {/* Gradient background */}
      <div className="absolute inset-0 bg-gradient-to-br from-[#1a1a1a] via-[#121212] to-[#0a0a0a]" />
    </div>
  );
}